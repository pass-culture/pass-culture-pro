const MAINTENANCE_ERRORS = 'MAINTENANCE_ERRORS'

const initialState = {}

const maintenanceErrors = (state = initialState, action = {}) => {
  console.log('********************')
  return state
}

export default maintenanceErrors
