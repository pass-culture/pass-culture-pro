const initialState = {
  loading: false,
  backTo: null,
  fullscreen: false,
  header: {},
  pageName: 'todo',
  redBg: false,
  whiteHeader: false,
  withLoading: false,
}

const app = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state
  }
}

export default app
